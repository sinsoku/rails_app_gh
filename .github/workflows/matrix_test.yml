name: matrix test

on: push

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    outputs:
      name: ${{ matrix.name }}

    strategy:
      fail-fast: false
      matrix:
        name: [foo, bar]

    steps:
    - name: echo matrix variables
      run: |
        echo ${{ matrix.name }}

  after:
    needs: run-foo
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
    - run: |
        echo "${{ needs.run.outputs.name }} OK"
